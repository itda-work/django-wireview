"""Example applications.

Each subdirectory is one wireview concept, small enough to read in a sitting and
wired into the Django project under tests/ so that `make test` runs them. An
example that CI does not run is an example that rots.
"""
